BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
BBFILE_COLLECTIONS += "meta-debug"
BBFILE_PATTERN_meta-debug = "^${LAYERDIR}/"
LAYERSERIES_COMPAT_meta-debug = "kirkstone scarthgap"
