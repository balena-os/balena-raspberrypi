# Symbols that balena-os-drivers.cfg cannot carry because they only exist on
# 5.15 only. Added to SRC_URI by linux-raspberrypi_5.15.bbappend.
#
# fb_tft_watterott was dropped from the fbtft staging driver after 5.17, which
# is why the old 6.12 bbappend gated it behind
#   configure_from_version("5.17", "", " fb_tft_watterott", d)
# i.e. enabled only below 5.17.
CONFIG_FB_TFT_WATTEROTT=m
