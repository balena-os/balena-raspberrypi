BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
BBFILE_COLLECTIONS += "meta-audit"
BBFILE_PATTERN_meta-audit = "^${LAYERDIR}/"
LAYERSERIES_COMPAT_meta-audit = "kirkstone scarthgap"